name = "unaovejafeliz"
compatibility_date = "2026-08-06"
pages_build_output_dir = "."

# Los secretos (CALENDAR_ID, GOOGLE_SA_*, TURNSTILE_SECRET) NO van aquí.
# En local: fichero `.dev.vars` (ignorado por git).
# En producción: Cloudflare Pages → Settings → Variables and Secrets (tipo Secret).
#
# Desarrollo local:  npx wrangler pages dev .
# Cargar un secret:  npx wrangler pages secret put GOOGLE_SA_PRIVATE_KEY
